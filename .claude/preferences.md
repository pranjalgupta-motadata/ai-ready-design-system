# User Preferences

## Git Workflow

**IMPORTANT**:

- **NEVER** run `git commit` commands
- **NEVER** run `git add` commands
- **NEVER** run `git push` commands
- User manages all git operations themselves
- Only provide solutions and implementations
- User decides when and how to commit changes

## My Role

- Provide code solutions
- Implement features and fixes
- Run tests and builds
- Answer questions
- **Let the user handle all git operations**
